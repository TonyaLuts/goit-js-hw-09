function createPromise(position, delay) {
  const shouldResolve = Math.random() > 0.3;
  if (shouldResolve) {
    // Fulfill
  } else {
    // Reject
  }
}
const date = new Date();
console.log(date);

const date2 = Date.now();
console.log(date2);
